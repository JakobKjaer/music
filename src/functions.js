// I denne fil kan man eksportere vores funktioner til andre komponenter inden i functions.js
// Man importere blot dette inden i sin komponent "import { playAudio } from '../functions';"
// export const playAudio = (playingStatus, audioRef) => {
// 	if (playingStatus) {
// 		const playingSong = audioRef.current.play();
// 		if (playingSong !== undefined) {
// 			playingSong.then((audio) => {
// 				audioRef.current.play();
// 			});
// 		}
// 	}
// };
